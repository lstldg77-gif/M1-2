# 보너스 과제 구현 설명

## 1. 구현 현황 요약

현재 프로젝트는 보너스 과제 중 **인사이트·UX 고도화 항목을 구현**했습니다.

- 시계열 데이터 요약 및 통계
- Chart.js 기반 추세 그래프
- CSV/JSON 데이터 내보내기
- 다크 모드 토글
- 음악 개수 선택 UI
- Suno 음원 일괄 생성
- 음원 미리듣기·다운로드·PC 저장·웹 목록 삭제

AI Function Calling과 MCP Server/GPT Actions 연동은 현재 구현하지 않았습니다.

## 2. 시계열 데이터 분석과 활용 흐름

사용자가 날짜, 컨디션 점수, 메모를 입력하면 `/api/data` API가 데이터를 저장합니다. 저장소는 Firestore를 우선 사용하고, Firestore를 사용할 수 없으면 로컬 JSON 저장소를 사용합니다.

`/api/data/summary`는 저장된 데이터를 분석해 다음 정보를 반환합니다.

- 기록 기간
- 총 기록 수
- 총합
- 평균
- 최댓값
- 최솟값
- 최근 추세

이 요약 정보는 데이터 관리 화면의 통계 카드와 Chart.js 그래프에 사용되며, AI 채팅의 시스템 프롬프트에도 주입됩니다.

```mermaid
flowchart LR
    INPUT[날짜·점수·메모 입력] --> CRUD[/api/data CRUD]
    CRUD --> STORE[(Firestore 또는 local_db.json)]
    STORE --> SUMMARY[/api/data/summary]
    SUMMARY --> CHART[통계 카드·Chart.js 그래프]
    SUMMARY --> CONTEXT[AI 시스템 프롬프트]
```

## 3. FastAPI 라우터·서비스 분리

`main.py`는 FastAPI 앱, CORS, 정적 파일, 라우터를 조립하는 역할을 합니다.

기능별 HTTP 엔드포인트는 `routers/`에 두고, 외부 API 호출과 데이터 저장 같은 업무 로직은 `services/`에 분리했습니다.

- `routers/data_router.py`: 시계열 데이터 CRUD와 요약
- `routers/chat_router.py`: AI 대화
- `routers/conversations_router.py`: 대화 기록 CRUD
- `routers/music_router.py`: Suno 기획·생성·상태 조회·다운로드
- `services/firestore_service.py`: Firestore 및 로컬 JSON 저장
- `services/ai_service.py`: 컨텍스트 구성, AI 응답, Suno 프롬프트 기획
- `services/suno_service.py`: Apiframe Suno API 통신과 MP3 저장

이 구조의 장점은 API 경로와 업무 로직을 분리해 유지보수와 테스트를 쉽게 할 수 있다는 점입니다. 저장소나 외부 AI 서비스가 바뀌어도 라우터의 변경을 최소화할 수 있습니다.

## 4. Pydantic 요청 검증

FastAPI 엔드포인트의 요청 모델에 Pydantic을 사용했습니다. 클라이언트가 보낸 JSON의 필수 필드와 타입을 서비스 로직에 전달하기 전에 검증하기 위한 것입니다.

예시:

- `DataItemCreate`: `date`, `value`, `memo` 검증
- `DataItemUpdate`: 수정 가능한 필드 검증
- `MusicPlanRequest`: 기분, 컨디션, 요청사항, 가사 여부, 곡 수 검증
- `MusicGenerateRequest`: 음악 프롬프트, 태그, 제목, 가사 검증

필수 값이 누락되거나 타입이 잘못되면 FastAPI가 자동으로 `422` 검증 오류를 반환합니다. 따라서 서비스 계층은 반복적인 입력 검사보다 실제 업무 로직에 집중할 수 있습니다.

## 5. Firestore 저장 및 CRUD

Firestore를 사용할 수 있는 환경에서는 컬렉션과 문서를 이용합니다.

- 생성: 새 문서 생성 후 문서 ID 반환
- 조회: 컬렉션 목록 또는 특정 문서 조회
- 수정: 문서의 변경 필드 업데이트
- 삭제: 문서 삭제

Firestore 초기화가 되지 않는 개발 환경에서는 로컬 JSON 저장소로 자동 전환합니다. 같은 `/api/data` API를 유지하므로 개발 환경과 배포 환경에서 프런트엔드 사용 방식이 달라지지 않습니다.

## 6. 컨텍스트 주입 원리

AI에게 원본 데이터를 모두 전달하는 대신, 먼저 데이터 요약을 만든 뒤 시스템 프롬프트에 넣습니다.

```text
사용자 메시지
  -> get_data_summary()
  -> 기간·평균·최고·최저·추세 계산
  -> 시스템 프롬프트에 컨텍스트 주입
  -> OpenAI 또는 Gemini 호출
  -> 사용자 상태를 반영한 AI 답변과 음악 제안
```

이 방식으로 AI는 단순한 일반 답변이 아니라 사용자의 최근 컨디션과 추세를 반영한 공감 대화와 음악 추천을 제공할 수 있습니다.

## 7. 배포 환경의 CORS·환경변수·키 관리

### CORS

프런트엔드와 백엔드가 서로 다른 주소에 배포될 수 있습니다. 예를 들어 프런트엔드는 Vercel, 백엔드는 Render에서 실행될 수 있으므로 브라우저의 교차 출처 요청을 허용해야 합니다.

운영 환경에서는 모든 출처를 허용하기보다 실제 프런트엔드 도메인만 허용하는 것이 안전합니다.

### 환경변수

개발 환경과 배포 환경의 API 주소, 데이터베이스, 저장 경로가 다르므로 설정값을 코드에 하드코딩하지 않고 환경변수로 분리합니다.

### API 키 관리

다음 값은 외부에 공개하면 안 됩니다.

- `OPENAI_API_KEY`
- `GEMINI_API_KEY`
- `APIFRAME_API_KEY`
- Firebase 인증 정보

키는 `.env` 또는 배포 플랫폼의 Secret 환경변수에만 저장하고, `.gitignore`를 통해 GitHub 커밋에서 제외합니다.

## 8. 구현된 UX 고도화 기능

### 통계와 시각화

데이터 관리 화면에서 기간, 기록 수, 평균, 최근 추세를 확인할 수 있고, Chart.js 선 그래프로 컨디션 변화를 볼 수 있습니다.

### 데이터 내보내기

브라우저에서 현재 데이터를 다음 형식으로 내려받을 수 있습니다.

- CSV: 스프레드시트 분석용
- JSON: 다른 프로그램이나 백업용

### 다크 모드

상단 테마 버튼으로 다크 테마와 라이트 테마를 전환할 수 있습니다.

### Suno 음악 선택 UI

다음 조건을 선택해 음악을 기획할 수 있습니다.

- 생성 곡 수: 1개, 5개, 10개, 15개
- 가사 포함 또는 Instrumental
- 주제
- 현재 기분
- 현재 컨디션
- 악기 및 추가 요청

생성된 음원은 브라우저에서 미리듣기하고 다운로드할 수 있습니다. 웹 목록에서 곡을 삭제해도 PC `Output/YYYY-MM-DD/` 폴더의 MP3 파일은 삭제하거나 수정하지 않습니다.

## 9. 미구현 보너스 항목

### AI Function Calling

현재 AI는 `get_data_summary()`를 내부에서 직접 호출한 뒤 결과를 시스템 프롬프트에 주입합니다. GPT가 도구 목록을 보고 필요할 때 자동으로 도구를 선택 호출하는 Function Calling 스키마는 아직 연결하지 않았습니다.

### MCP Server 또는 GPT Actions

외부 클라이언트가 MCP 도구 또는 GPT Actions를 통해 `/api/data/summary`, `/api/conversations` 등의 API를 호출하는 연동도 아직 구현하지 않았습니다.

## 10. 발표용 핵심 답변

> 이 프로젝트는 날짜별 컨디션 데이터를 Firestore 또는 로컬 JSON에 저장하고, FastAPI의 `/api/data/summary`에서 통계와 최근 추세를 계산합니다. 계산된 요약 정보는 Chart.js 시각화와 AI 시스템 프롬프트의 컨텍스트로 재사용됩니다. FastAPI는 라우터와 서비스를 분리해 API 계층과 업무 로직을 나누었고, Pydantic으로 요청 형식을 검증했습니다. 배포 시에는 CORS로 프런트엔드와 백엔드 간 요청을 제어하고, API 키와 Firebase 인증 정보는 환경변수와 Secret으로 관리합니다.
