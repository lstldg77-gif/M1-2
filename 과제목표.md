# 과제 목표

## 1. 프로젝트 전체 목표

사용자의 날짜별 컨디션과 기분 데이터를 저장·분석하고, 그 결과를 바탕으로 맞춤형 AI 대화와 음악 제작을 지원하는 서비스를 구현한다.

최종적으로 사용자가 다음 과정을 하나의 서비스에서 수행할 수 있도록 한다.

1. 날짜별 컨디션·기분·메모 기록
2. 기록 데이터의 통계와 추세 확인
3. 최근 상태를 반영한 AI 상담 및 음악 추천
4. Suno AI 음악 프롬프트 생성
5. 선택한 곡 수만큼 음원 자동 생성
6. 생성된 음원 미리듣기·다운로드·PC 저장
7. 음악의 제목·가사·스타일을 활용한 Google Flow 이미지·영상 제작

## 2. 기능 구현 목표

### 데이터 관리

- 날짜, 점수, 메모를 입력할 수 있어야 한다.
- 데이터를 생성, 조회, 수정, 삭제할 수 있어야 한다.
- Firestore를 사용할 수 있어야 한다.
- Firestore가 없을 때 로컬 JSON 저장소로 동작해야 한다.

### 데이터 분석

- 기록 기간을 계산한다.
- 전체 기록 수를 제공한다.
- 총합, 평균, 최댓값, 최솟값을 계산한다.
- 최근 데이터의 변화 추세를 제공한다.
- 분석 결과를 화면의 통계 카드와 그래프로 표시한다.

### AI 비서

- 사용자의 최근 데이터 요약을 AI에 전달한다.
- 데이터 요약을 시스템 프롬프트에 주입한다.
- 사용자의 컨디션과 기분에 맞는 공감형 답변을 제공한다.
- 음악 테마와 Suno 프롬프트를 추천한다.
- 대화 내용을 저장하고 다시 불러올 수 있어야 한다.

### Suno 음악 제작

- 생성할 곡 수를 `1/5/10/15개` 중 선택한다.
- 가사 포함 보컬 곡과 Instrumental을 선택한다.
- 테마, 기분, 컨디션, 악기와 추가 요청을 입력한다.
- 선택한 곡 수만큼 프롬프트를 생성한다.
- 생성된 프롬프트를 Suno API에 순차적으로 전달한다.
- 음원 생성 상태를 확인한다.
- 완성된 음원을 브라우저에서 미리듣기한다.
- 음원을 웹에서 다운로드하거나 PC의 날짜별 폴더에 저장한다.
- 웹 목록에서 곡을 삭제해도 PC에 저장된 MP3는 유지한다.

## 3. 웹 서비스 목표

- FastAPI 기반 백엔드와 반응형 웹 화면을 연결한다.
- AI 대화, 데이터 관리, Suno 스튜디오, 대화 기록을 탭으로 구성한다.
- Chart.js로 컨디션 변화 그래프를 제공한다.
- CSV와 JSON 데이터 내보내기를 제공한다.
- 다크 모드와 라이트 모드를 제공한다.
- API 키와 Firebase 인증 정보를 코드에 노출하지 않는다.
- 로컬 실행과 Render/Vercel 배포를 고려한 구조로 구성한다.

## 4. 기술 설계 목표

### 계층 분리

- `main.py`: FastAPI 애플리케이션 조립
- `routers/`: HTTP 요청과 응답 처리
- `services/`: 데이터 저장, AI 호출, Suno API 호출
- `schemas.py`: Pydantic 요청·응답 모델
- `index.html`, `app.js`, `styles.css`: 웹 사용자 인터페이스

### 요청 검증

Pydantic 모델을 사용해 필수 입력값, 데이터 타입, API 요청 형식을 검증한다. 잘못된 요청은 서비스 로직에 도달하기 전에 오류로 반환해 안정성을 높인다.

### 저장소 폴백

운영 환경에서는 Firestore를 사용하고, 로컬 개발이나 인증 정보가 없는 환경에서는 JSON 파일을 사용한다. 저장소가 달라져도 프런트엔드 API 계약은 동일하게 유지한다.

### 보안과 배포

- `.env`에 API 키를 저장한다.
- `.env`를 `.gitignore`로 제외한다.
- MP3와 개인 생성 결과는 GitHub에 올리지 않는다.
- CORS 설정으로 프런트엔드와 백엔드의 통신 출처를 관리한다.
- 배포 환경에서는 플랫폼의 Secret 환경변수를 사용한다.

## 5. 보너스 과제 목표

### 인사이트·UX 고도화

- 통계 지표를 하나 이상 확장한다.
- 데이터 변화 그래프를 제공한다.
- CSV 또는 JSON 내보내기를 제공한다.
- 다크 모드 토글을 제공한다.
- 사용자가 음악 개수와 가사 여부를 선택할 수 있게 한다.

### AI 도구 호출 확장 목표

추가 확장 단계에서는 GPT Function Calling을 적용한다.

- 데이터 요약 도구 스키마 정의
- 대화 조회 도구 스키마 정의
- GPT가 질문에 따라 필요한 도구를 선택 호출
- 도구 결과를 다시 AI 답변에 반영
- 호출한 도구와 호출 근거를 README에 기록

### 외부 채널 연동 목표

MCP Server 또는 GPT Actions 중 한 가지 방식을 선택해 외부 클라이언트에서도 다음 기능을 호출할 수 있도록 확장한다.

- 컨디션 데이터 조회
- 데이터 요약 조회
- 대화 기록 조회
- 음악 프롬프트 기획

## 6. 최종 결과물 목표

최종 결과물은 다음 조건을 만족해야 한다.

- 웹 브라우저에서 실행된다.
- 로컬 데이터만으로도 기본 기능을 확인할 수 있다.
- API 키가 없을 때도 폴백 또는 안내 메시지가 제공된다.
- 사용자의 데이터와 생성 음원을 안전하게 관리한다.
- README에 실행 방법과 설계 근거가 정리되어 있다.
- Google Flow에서 사용할 수 있는 제목·가사·스타일·장면 프롬프트 자료가 제공된다.

## 7. 한 문장 요약

> 사용자의 컨디션 데이터를 분석해 AI가 상황에 맞는 대화와 음악을 기획하고, Suno 음원 생성부터 미리듣기·저장·Flow 영상 제작 자료 정리까지 연결하는 개인 맞춤형 AI 음악 제작 서비스를 구현한다.
